# dev

TypeScript and bun typings for editor and typecheck support across all
plugins. Not shipped to plugin users — plugin scripts run directly via
`bun` with no dependencies of their own.

```bash
cd dev
bun install
bun run typecheck
```
